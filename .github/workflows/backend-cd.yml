name: Backend Service Continuous Delivery

on:
  pull_request:
    branches:
      - main # Run the workflow only on pull requests to the main branch
    paths:
      - 'backend/**' # Workflow triggered on changes to code
      - '.github/workflows/backend_ci.yml'

jobs:

  sd-1-build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'corretto'

      - name: Build with Maven
        run: mvn --no-transfer-progress clean install -q -DskipTests -T 5

      - name: Upload built artifact for future usage
        uses: actions/upload-artifact@v4.3.3
        with:
          name: sprawler-backend-artifact
          path: 'backend/target/*.war'

  sd-2-containerize:
    runs-on: ubuntu-latest
    needs: ["sd-1-build"]

    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Download built artifact
        uses: actions/download-artifact@v4
        with:
          name: sprawler-backend-artifact

      - name: Build Docker image
        run: docker build -t sprawler-backend:latest .

      - name: Upload built Docker image as artifact
        uses: actions/upload-artifact@v4.3.3
        with:
          name: sprawler-backend-docker-image
          path: 'sprawler-backend:latest'

  # Upload Docker image to GCP GCR
  sd-3a-upload-gcp-gcr:
    runs-on: ubuntu-latest
    needs: ["sd-2-containerize"]

    steps:
      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: sprawler-backend-docker-image

#      - name: Log in to GCP Container Registry
#        uses: google-github-actions/auth@v1
#        with:
#          credentials_json: ${{ secrets.GCP_CREDENTIALS }}
#
#      - name: Push Docker image to GCP Container Registry
#        run: |
#          docker tag sprawler-backend:latest gcr.io/${{ secrets.GCP_PROJECT_ID }}/sprawler-backend:latest
#          docker push gcr.io/${{ secrets.GCP_PROJECT_ID }}/sprawler-backend:latest


  # Upload Docker image to AWS ECR
  sd-3b-upload-aws-ecr:
    runs-on: ubuntu-latest
    needs: ["sd-2-containerize"]

    steps:
      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: sprawler-backend-docker-image

  # Upload Docker image to Azure ACR
  sd-3c-upload-azure-acr:
    runs-on: ubuntu-latest
    needs: ["sd-2-containerize"]

    steps:
      - name: Download Docker image artifact
        uses: actions/download-artifact@v4
        with:
          name: sprawler-backend-docker-image